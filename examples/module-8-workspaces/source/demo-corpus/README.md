# Module 8 demo corpus — workspaces v2

Every value the v2 video shows traces to one of these files. Nothing on
screen is invented.

- [credential-share.md](credential-share.md) — the docs' own credential-share
  example (`CREDENTIAL_SHARE_WORKFLOW` from
  `~/sim/sim/apps/docs/components/workflow-preview/examples.ts`), plus the
  credential `displayName` example from the Credential block reference page.
  Supports scene 5 (one credential, three blocks) and scene 4 (the Google
  account that opens the port).

Workspace name: **beaming-polaris** — the real workspace the module-5
artifacts were captured from (`module-5-agents/demo-corpus/triage-run.md`).
Reused so the whole series stays in one world.
