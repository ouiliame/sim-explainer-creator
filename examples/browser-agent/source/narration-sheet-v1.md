# Browser Agent ("The Agent With Hands") — narration sheet (v1)

Total runtime: 82.4s (after vo-sync; visual minimums 70s — the deltas are
end-holds). Scene boundaries are hard cuts at the listed times. Visuals
are authoritative: re-time VO to the picture, not the reverse. A scratch
VO (narration-v1.md, draft register — yours to rewrite) is synced and
mounted; massage the prose and re-run vo-sync to re-time.

## 1. the-task — 0:00.0–0:08.7

- **Beat intent:** a normal Sim workflow — one agent between Start and
  Response, told to research whatever comes in; the brief is its output.
- **Key moments:** t≈0.4–3.6 chain assembles in docs order; t≈4.8–6.4 the
  `<start.input>` tag glows (it's a reference) and releases.
- **Room:** settled frame from ~6.5s.

## 2. the-toolbelt — 0:08.7–0:19.7

- **Beat intent:** the toolbelt is a reach ladder — Exa finds, Firecrawl
  reads, Browser Use acts (a real browser). The agent decides when each
  is worth calling.
- **Key moments:** t≈0.6 selection ring on Research; t≈1.0 Tools row
  grows; chips land + ring-pulse at ≈2.2 (Exa), ≈3.8 (Firecrawl), ≈5.9
  (Browser Use, on its own line); ring releases 7.0.
- **Room:** settled from ~7.2s.

## 3. the-run-begins — 0:19.7–0:33.3

- **Beat intent:** ONE run starts (it spans the next three scenes — the
  live ring never releases); the agent's first move is a web search, and
  the result comes back as a captured card — the first evidence.
- **Key moments:** t≈0.4 Start blips + Input glows; t≈1.0 pulse crosses;
  t≈1.6 live ring ON (held to the cut); t≈2.0–3.6 Messages tag glows;
  t≈4.4 Exa chip rings and card 1 drops IN SYNC (slot 1, search rows);
  green capture pulse ≈5.8–7.5.
- **Room:** from ~7.6s (live ring holding).

## 4. reading-the-pages — 0:33.3–0:42.3

- **Beat intent:** one Firecrawl call per source worth reading — the
  page comes back as clean text; the strip grows as it reads (second
  call lands at ~1.6× tempo).
- **Key moments:** ring+card 2 at t≈0.8–3.8; ring+card 3 at t≈4.0–6.6.
- **Room:** from ~6.6s.

## 5. hands-on-the-web — 0:42.3–1:02.6

- **Beat intent:** the set piece. Some pages have to be USED — Browser
  Use is a live session (the block literally outputs a liveUrl) that
  navigates, clicks, and collects "every plan name and monthly price"
  (the registry's own task wording); the finished session folds into the
  rail as evidence card 4.
- **Key moments:** t≈0.5 Browser Use chip rings (HELD ~14s); t≈1.4 the
  viewport rises over the dimmed world; t≈4.3 click → pricing page;
  t≈6.7/8.5/10.0 plan clicks with blue capture glows settling green;
  t≈12.6–14.0 the FOLD into slot 4; t≈14.0 green landing pulse; world
  undims; live ring still held.
- **Room:** ~15.5s onward.

## 6. the-brief-comes-back — 1:02.6–1:11.6

- **Beat intent:** the evidence feeds one answer — the agent assembles
  the brief from its captured results and the run completes through
  Response; `<research.content>` is where the brief travels.
- **Key moments:** t≈0.8–4.0 the four cards glow in call order; t≈3.8
  Research settles green; t≈4.0 pulse; t≈4.9–6.4 the Data-row reference
  glows; t≈6.4 Response green. Settled rings hold through the cut.
- **Room:** from ~6.6s.

## 7. the-evidence-trail — 1:11.6–1:22.4

- **Beat intent:** closure — one prompt, one run, work done on the live
  web, receipts on the rail.
- **Key moments:** t≈0.8–2.2 camera eases back; t≈2.6–5.8 the four cards
  take one quiet recap pulse each, in call order.
- **Anchor (director's mandate):** "the agent with hands" · "an agent
  that doesn't just answer — it DOES" (mandate wording; the drafted VO
  avoids the contrastive form per the register rules — yours to call).
- **Room:** from ~6s to the end.
